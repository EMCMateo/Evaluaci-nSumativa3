import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'EvaluacionSumativa_MateoMolina3E3',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
